export default function BottomTaskBar() {
  return (
    <div className="fixed bottom-0 left-0 right-0 z-[40] bg-slate-950/90 backdrop-blur-md border-t border-white/10 h-12 flex items-center justify-center px-4 pointer-events-none">
      {/* Empty task bar - reserved for future features */}
    </div>
  );
}
