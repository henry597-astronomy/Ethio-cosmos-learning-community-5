import { readFile } from 'node:fs/promises';

const files = Object.fromEntries(
  await Promise.all([
    ['groqClient', 'src/services/groq.ts'],
    ['groqApi', 'api/groq/chat.ts'],
    ['voiceApi', 'api/voice/transcribe.ts'],
    ['tokenApi', 'api/livekit/token.ts'],
    ['stopApi', 'api/livekit/stop-hosting.ts'],
    ['liveKitContext', 'src/context/LiveKitContext.tsx'],
    ['adminClassrooms', 'src/components/ClassroomAdminPanel.tsx'],
    ['security', 'api/_lib/security.ts'],
    ['migration', 'supabase/security_hardening.sql'],
    ['serviceWorker', 'public/sw.js'],
    ['offlineCache', 'src/lib/offline-cache.ts'],
    ['offlinePrefetch', 'src/lib/background-prefetch.ts'],
    ['offlineButton', 'src/components/OfflineSaveButton.tsx'],
    ['topicOfflineButton', 'src/components/OfflineTopicSaveButton.tsx'],
    ['materialsPage', 'src/pages/MaterialsPage.tsx'],
    ['materialDownload', 'src/lib/material-download.ts'],
    ['downloadsPlugin', 'android/app/src/main/java/com/ethiocosmos/learning/DownloadsPlugin.java'],
    ['mainActivity', 'android/app/src/main/java/com/ethiocosmos/learning/MainActivity.java'],
    ['videoUtils', 'src/lib/video-utils.ts'],
    ['index', 'index.html'],
    ['premiumClient', 'src/context/PremiumContext.tsx'],
    ['premiumAdmin', 'src/components/PremiumAdminPanel.tsx'],
    ['premiumHelper', 'api/_lib/premium.ts'],
    ['premiumAccessApi', 'api/premium/access.ts'],
    ['premiumCheckoutApi', 'api/premium/checkout.ts'],
    ['premiumMigration', 'supabase/premium_mode.sql'],
    ['offlinePremiumMigration', 'supabase/offline_downloads_premium.sql'],
    ['premiumPlanPricing', 'supabase/premium_plan_pricing.sql'],
    ['manualPaymentMigration', 'supabase/manual_payment_instructions.sql'],
    ['paymentSubmissionMigration', 'supabase/premium_payment_submissions.sql'],
    ['paymentApprovalMigration', 'supabase/premium_payment_approval.sql'],
    ['paymentBackfillMigration', 'supabase/premium_payment_entitlement_backfill.sql'],
    ['notificationCleanupMigration', 'supabase/notification_cleanup.sql'],
    ['paymentSubmissionClient', 'src/components/PremiumPaymentSubmissionPanel.tsx'],
    ['paymentSubmissionAdmin', 'src/components/PremiumPaymentAdminPanel.tsx'],
    ['authContext', 'src/context/AuthContext.tsx'],
    ['premiumSecurityMigration', 'supabase/premium_security.sql'],
    ['premiumLessonMigration', 'supabase/premium_lessons.sql'],
    ['premiumContentMigration', 'supabase/premium_content_and_profile_repair.sql'],
    ['premiumAiClient', 'src/components/AIChatBar.tsx'],
    ['premiumSolarPage', 'src/pages/SolarSystemPage.tsx'],
    ['hostingPremiumMigration', 'supabase/live_hosting_premium.sql'],
    ['roomRemovalMigration', 'supabase/primary_admin_room_removal.sql'],
    ['notificationMigration', 'supabase/app_notifications.sql'],
    ['announceApi', 'api/notifications/announce.ts'],
    ['notificationContext', 'src/context/NotificationContext.tsx'],
    ['notificationCenter', 'src/components/NotificationCenter.tsx'],
    ['chatPage', 'src/pages/ChatPage.tsx'],
    ['r2Api', 'api/_lib/r2-shorts.ts'],
    ['r2Client', 'src/services/r2-shorts.ts'],
    ['shortsFeed', 'src/components/ShortsFeed.tsx'],
    ['r2Migration', 'supabase/shorts_r2.sql'],
  ].map(async ([key, path]) => [key, await readFile(path, 'utf8')]))
);

const checks = [
  ['Groq client has no browser key or direct provider URL', !/VITE_GROQ_API_KEY|api\.groq\.com|atob\(/.test(files.groqClient)],
  ['Groq API uses server-only key', /process\.env\.GROQ_API_KEY/.test(files.groqApi)],
  ['Groq API authenticates, Premium-gates, validates, and rate-limits requests', /authenticateSupabaseRequest/.test(files.groqApi) && /requirePremiumFeature\(auth\.client, 'ai_tutor'\)/.test(files.groqApi) && /enforceRateLimit/.test(files.groqApi)],
  ['Voice transcription Premium-gates before provider usage', /authenticateSupabaseRequest/.test(files.voiceApi) && /requirePremiumFeature\(auth\.client, 'ai_tutor'\)/.test(files.voiceApi)],
  ['LiveKit token verifies the authenticated user', /authenticateSupabaseRequest/.test(files.tokenApi) && /Authorization/.test(files.security)],
  ['LiveKit token does not trust client identity metadata', !/requestBody\.(userName|userId|avatarUrl)/.test(files.tokenApi)],
  ['LiveKit viewers cannot publish media or room data', /canPublish: isHost/.test(files.tokenApi) && /canPublishData: isHost/.test(files.tokenApi)],
  ['LiveKit shutdown binds to authenticated owner', /auth\.user\.id/.test(files.stopApi) && /eq\('host_id', auth\.user\.id\)/.test(files.stopApi)],
  ['Live viewers only receive heartbeat-fresh sessions', /SESSION_FRESHNESS_MS/.test(files.liveKitContext) && /freshSessions/.test(files.liveKitContext) && /setActiveSessions/.test(files.liveKitContext)],
  ['LiveKit rejects stale viewer token requests', /SESSION_FRESHNESS_MS/.test(files.tokenApi) && /if \(!isHost\)/.test(files.tokenApi) && /last_heartbeat/.test(files.tokenApi) && /\.gte\(/.test(files.tokenApi)],
  ['Admin room management retains raw active sessions for removal', /allActiveSessions/.test(files.adminClassrooms) && /removeRoom/.test(files.adminClassrooms) && /removeLiveClassroom/.test(files.adminClassrooms)],
  ['Profile privilege escalation is blocked by a trigger', /protect_profile_security_fields/.test(files.migration) && /Only the primary administrator/.test(files.migration)],
  ['Unrestricted storage uploads are removed', /DROP POLICY IF EXISTS "Authenticated Upload Access"/.test(files.migration) && /public\.is_active_admin\(\)/.test(files.migration)],
  ['Private API data is not in the service-worker public cache list', !/channel_posts|channel_comments|channel_reactions|comment_reactions/.test(files.serviceWorker.match(/const PUBLIC_API_PATTERNS = \[[\s\S]*?\];/)?.[0] ?? '')],
  ['Offline reconnect never starts a global content download', /refreshSelectedOfflineContent/.test(files.offlinePrefetch) && !/setupOnlineListener[\s\S]{0,500}downloadOfficialLearningPack/.test(files.offlinePrefetch) && /Broad offline prefetch is disabled/.test(files.serviceWorker)],
  ['Offline detail reads require explicit current-user manifest records', /isOfflineDetailKey/.test(files.offlineCache) && /contentKeys\.includes/.test(files.offlineCache) && /userId/.test(files.offlineCache) && /language/.test(files.offlineCache)],
  ['Offline download controls are Premium-gated and explicit', /offline_learning_packs/.test(files.offlineButton) && /offline_learning_packs/.test(files.topicOfflineButton) && /saveSelectedTopicOffline/.test(files.topicOfflineButton) && /saveSelectedMaterialOffline/.test(files.offlineButton)],
  ['Materials Document category maps to PDF records without a legacy bypass download', /document/.test(files.materialsPage) && /section\.pdfs/.test(files.materialsPage) && !/link\.download/.test(files.materialsPage)],
  ['Material export targets Downloads and reports export failure separately', /MediaStore\.Downloads/.test(files.downloadsPlugin) && /DIRECTORY_DOWNLOADS/.test(files.downloadsPlugin) && /Downloads\.save/.test(files.materialDownload) && /offlineSavedButExportFailed/.test(files.offlineButton)],
  ['Native Downloads bridge is registered without replacing the existing activity', /registerPlugin\(DownloadsPlugin\.class\)/.test(files.mainActivity) && /extends BridgeActivity/.test(files.mainActivity)],
  ['Hosted video links provide derived previews when thumbnails are absent', /getVideoPreviewUrl/.test(files.videoUtils) && /img\.youtube\.com/.test(files.videoUtils) && /vumbnail\.com/.test(files.videoUtils) && /getVideoPreviewUrl\(video\.url\)/.test(files.materialsPage)],
  ['Startup errors are rendered as text, not HTML', !/errDiv\.innerHTML/.test(files.index) && /details\.textContent/.test(files.index)],
  ['Premium client contains no provider secrets', !/CHAPA_SECRET_KEY|ARIFPAY_SECRET_KEY|PREMIUM_PROVIDER_WEBHOOK_SECRET/.test(files.premiumClient + files.premiumAdmin)],
  ['Premium server guard uses the ownership-safe RPC', /user_has_premium_feature/.test(files.premiumHelper) && /requested_feature/.test(files.premiumHelper) && !/requested_user/.test(files.premiumHelper)],
  ['AI client sends the signed-in bearer token', /Authorization: `Bearer \$\{accessToken\}`/.test(files.groqClient)],
  ['Premium lesson flags are Admin-write-only and protect lesson rows', /premium_lessons/.test(files.premiumLessonMigration) && /Admins manage premium lesson flags/.test(files.premiumLessonMigration) && /user_has_premium_lesson/.test(files.premiumLessonMigration) && /DROP POLICY IF EXISTS "Public read access for lessons"/.test(files.premiumLessonMigration)],
  ['Premium content hierarchy is categorized and Admin-write-only', /CREATE TABLE IF NOT EXISTS public\.premium_topics/.test(files.premiumContentMigration) && /CREATE TABLE IF NOT EXISTS public\.premium_subtopics/.test(files.premiumContentMigration) && /Admins manage premium topic flags/.test(files.premiumContentMigration) && /Admins manage premium subtopic flags/.test(files.premiumContentMigration) && /user_can_access_learning/.test(files.premiumContentMigration)],
  ['Profile repair is current-user-only and preserves role safeguards', /CREATE OR REPLACE FUNCTION public\.ensure_current_profile/.test(files.premiumContentMigration) && /GRANT EXECUTE ON FUNCTION public\.ensure_current_profile\(\) TO authenticated/.test(files.premiumContentMigration) && /unique_profile_username/.test(files.premiumContentMigration)],
  ['Premium UI uses the exact seeded feature keys', /canUse\('ai_tutor'\)/.test(files.premiumAiClient) && /canUse\('observatory_simulation'\)/.test(files.premiumSolarPage)],
  ['Premium access API authenticates and rate-limits requests', /authenticateSupabaseRequest/.test(files.premiumAccessApi) && /enforceRateLimit/.test(files.premiumAccessApi)],
  ['Premium checkout fails closed while provider is not ready', /PAYMENT_PROVIDER_NOT_CONNECTED/.test(files.premiumCheckoutApi) && /readyForCheckout/.test(files.premiumCheckoutApi)],
  ['Manual payment instructions are additive, public-data-only, and never store secrets', /ALTER TABLE public\.premium_settings/.test(files.manualPaymentMigration) && /manual_payment_enabled/.test(files.manualPaymentMigration) && /manual_payment_receiver_name/.test(files.manualPaymentMigration) && /manual_payment_account/.test(files.manualPaymentMigration) && /Never store passwords, PINs, OTPs, API keys, or card data/.test(files.manualPaymentMigration) && !/\b(?:password|pin|otp|cvv|cvc|api[_ -]?key|secret)\b\s+(?:text|varchar|jsonb)/i.test(files.manualPaymentMigration)],
  ['Payment proofs are private, bounded, ownership-scoped, and admin-reviewed', /CREATE TABLE IF NOT EXISTS public\.premium_payment_submissions/.test(files.paymentSubmissionMigration) && /proof_size_bytes <= 10485760/.test(files.paymentSubmissionMigration) && /INSERT INTO storage\.buckets[\s\S]*?'payment-proofs'[\s\S]*?false,[\s\S]*?10485760/.test(files.paymentSubmissionMigration) && /Users can create their own payment submissions/.test(files.paymentSubmissionMigration) && /Admins review payment submissions/.test(files.paymentSubmissionMigration) && /Only payment submission review fields can be changed/.test(files.paymentSubmissionMigration)],
  ['Approved payment review creates one audited entitlement and repairs legacy approvals', /review_premium_payment_submission/.test(files.paymentApprovalMigration) && /sync_approved_premium_payment_submission/.test(files.paymentApprovalMigration) && /payment_submission_id/.test(files.paymentApprovalMigration) && /ON CONFLICT \(payment_submission_id\)/.test(files.paymentApprovalMigration) && /public\.is_active_admin\(\)/.test(files.paymentApprovalMigration) && /premium_entitlements/.test(files.paymentBackfillMigration)],
  ['Native auth does not redirect on unrelated app-resume or file-picker events', !/window\.location\.hash = '\/'/.test(files.authContext) && /onAuthStateChange/.test(files.authContext) && /handleDeepLink/.test(files.authContext)],
  ['Payment-proof client uses allowlisted files and no public proof URL', /ACCEPTED_PROOF_TYPES/.test(files.paymentSubmissionClient) && /MAX_PROOF_SIZE/.test(files.paymentSubmissionClient) && /from\('payment-proofs'\)/.test(files.paymentSubmissionClient) && !/getPublicUrl/.test(files.paymentSubmissionClient)],
  ['Payment-proof admin view uses short-lived signed URLs and grants access only through the verified RPC', /createSignedUrl/.test(files.paymentSubmissionAdmin) && /10 \* 60/.test(files.paymentSubmissionAdmin) && /review_premium_payment_submission/.test(files.paymentSubmissionAdmin) && /sync_approved_premium_payment_submission/.test(files.paymentSubmissionAdmin) && /Only payment submission review fields can be changed/.test(files.paymentApprovalMigration)],
  ['Notification cleanup is scoped to the signed-in user', /Users can delete their own app notifications/.test(files.notificationCleanupMigration) && /auth\.uid\(\) = user_id/.test(files.notificationCleanupMigration) && /GRANT DELETE ON TABLE public\.app_notifications TO authenticated/.test(files.notificationCleanupMigration) && /\.from\('app_notifications'\)[\s\S]*?\.delete\(\)[\s\S]*?\.eq\('user_id', user\.id\)/.test(files.notificationContext)],
  ['Notification removal is immediate and per-item without a warning dialog', /removeNotification/.test(files.notificationCenter) && /<X/.test(files.notificationCenter) && /notification\.id/.test(files.notificationCenter) && !/window\.confirm\(t\('clearNotificationsConfirm'\)\)/.test(files.notificationCenter)],
  ['Channel distinguishes network errors from a confirmed empty feed', /channelLoading/.test(files.chatPage) && /channelLoadError/.test(files.chatPage) && /channelLoading && posts\.length === 0/.test(files.chatPage) && /channelLoadError && posts\.length === 0/.test(files.chatPage) && /No channel posts yet/.test(files.chatPage)],
  ['Premium entitlement writes are Admin-only', /Admins manage premium entitlements/.test(files.premiumMigration) && /public\.is_active_admin\(\)/.test(files.premiumMigration) && /public\.is_active_admin\(\)/.test(files.paymentApprovalMigration)],
  ['Admin can edit Premium plan values and canonical prices are configured', /updatePlan/.test(files.premiumAdmin) && /price_birr/.test(files.premiumAdmin) && /premium_monthly/.test(files.premiumPlanPricing) && /premium_annual/.test(files.premiumPlanPricing) && /50, 30, true/.test(files.premiumPlanPricing) && /300, 365, true/.test(files.premiumPlanPricing)],
  ['Premium context refreshes entitlement state after approval', /visibilitychange/.test(files.premiumClient) && /addEventListener\('focus'/.test(files.premiumClient) && /setInterval/.test(files.premiumClient)],
  ['Premium RPC removes the arbitrary-user argument', /DROP FUNCTION IF EXISTS public\.user_has_premium_feature\(text, uuid\)/.test(files.premiumSecurityMigration) && /CREATE OR REPLACE FUNCTION public\.user_has_premium_feature\(requested_feature text\)/.test(files.premiumSecurityMigration) && !/requested_user/.test(files.premiumSecurityMigration)],
  ['LiveKit hosting uses the existing Premium feature guard', /isHost/.test(files.tokenApi) && /requirePremiumFeature\(auth\.client, 'live_stream_hosting'\)/.test(files.tokenApi) && /'status' in hostingAccess/.test(files.tokenApi)],
  ['Permanent room removal is primary-Admin-only and service-role bounded', /authenticateSupabaseRequest/.test(files.stopApi) && /operation === 'permanent_remove'/.test(files.stopApi) && /is_primary_admin/.test(files.stopApi) && /SUPABASE_SERVICE_ROLE_KEY/.test(files.stopApi) && /live_sessions/.test(files.stopApi)],
  ['Production classroom DELETE policy is primary-Admin-only', /DROP POLICY IF EXISTS "Admins can delete classrooms"/.test(files.roomRemovalMigration) && /public\.is_primary_admin\(\)/.test(files.roomRemovalMigration)],
  ['Live hosting feature is seeded free by default', /live_stream_hosting/.test(files.hostingPremiumMigration) && /is_premium[\s\S]*false/.test(files.hostingPremiumMigration)],
  ['Announcement API authenticates Admins and writes through service role', /authenticateSupabaseRequest/.test(files.announceApi) && /is_active_admin/.test(files.announceApi) && /SUPABASE_SERVICE_ROLE_KEY/.test(files.announceApi) && /app_notifications/.test(files.announceApi)],
  ['App notifications have own-row RLS and immutable content fields', /Users can read their own app notifications/.test(files.notificationMigration) && /Only notification read state can be changed/.test(files.notificationMigration) && /protect_app_notification_fields/.test(files.notificationMigration)],
  ['Channel posts create durable per-user notifications', /channel_posts_enabled/.test(files.notificationMigration) && /notify_channel_post/.test(files.notificationMigration) && /channel_post_notification_fanout/.test(files.notificationMigration) && /channel_post/.test(files.notificationMigration)],
  ['Notification permission is explicit rather than automatic at sign-in', !/Notification\.requestPermission\(\);/.test(files.notificationContext) && /requestBrowserNotificationPermission/.test(files.notificationContext)],
  ['R2 Shorts API uses server-only credentials and active-Admin auth', /authenticateSupabaseRequest/.test(files.r2Api) && /is_active_admin/.test(files.r2Api) && /process\.env\.R2_SECRET_ACCESS_KEY/.test(files.r2Api) && /getSignedUrl/.test(files.r2Api) && !/R2_SECRET_ACCESS_KEY|R2_ACCESS_KEY_ID/.test(files.r2Client)],
  ['R2 Shorts uploads enforce a bounded free-tier cap and verified object size', /HARD_FREE_TIER_CAP_BYTES/.test(files.r2Api) && /reserve_short_r2_upload/.test(files.r2Api) && /HeadObjectCommand/.test(files.r2Api) && /actualBytes/.test(files.r2Api) && /R2_QUOTA_REACHED/.test(files.r2Api)],
  ['New Shorts prefer R2 but preserve the legacy Supabase fallback', /createR2ShortUpload/.test(files.shortsFeed) && /isR2NotConfiguredError/.test(files.shortsFeed) && /supabase\.storage/.test(files.shortsFeed) && /finalizeR2ShortUpload/.test(files.shortsFeed)],
  ['R2 Shorts deletion is provider-aware and quota-accounted', /deleteR2Short/.test(files.shortsFeed) && /storage_provider === 'r2'/.test(files.shortsFeed) && /delete_short_r2_metadata/.test(files.r2Api) && /DeleteObjectCommand/.test(files.r2Api)],
  ['R2 metadata migration preserves legacy Supabase rows and hides server tables', /storage_provider TEXT NOT NULL DEFAULT 'supabase'/.test(files.r2Migration) && /CREATE TABLE IF NOT EXISTS public\.short_r2_usage/.test(files.r2Migration) && /CREATE TABLE IF NOT EXISTS public\.short_r2_uploads/.test(files.r2Migration) && /REVOKE ALL ON TABLE public\.short_r2_usage FROM PUBLIC, anon, authenticated/.test(files.r2Migration) && /GRANT EXECUTE ON FUNCTION public\.reserve_short_r2_upload/.test(files.r2Migration)],
  ['Offline downloading is seeded as a Premium feature', /offline_learning_packs[\s\S]{0,300}true/.test(files.premiumMigration) && /UPDATE public\.premium_features[\s\S]{0,100}SET is_premium = true[\s\S]{0,100}WHERE key = 'offline_learning_packs'/.test(files.offlinePremiumMigration)],
];

const failed = checks.filter(([, passed]) => !passed);
for (const [name, passed] of checks) {
  console.log(`${passed ? 'PASS' : 'FAIL'}: ${name}`);
}

if (failed.length > 0) process.exit(1);
